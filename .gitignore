# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Local hunt home (if ONEPIECE_HOME is pointed inside the repo)
.onepiece/

# Rust (reserved for a future optional fast path)
/target
**/*.rs.bk
Cargo.lock

# Local hunt state — seeds, counters, progress (never commit)
/state/
*.state.json
.onepiece/

# Secrets — never, ever commit a key or wallet
*.key
*.wif
wallet.dat
.env
.secrets/
found-*.txt

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
